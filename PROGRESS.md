# Risk Legacy — worklog

Dated, append-only history of what changed and why. **This is the tracker, not the plan.** Live task status and what to build next live in [BACKLOG.md](BACKLOG.md); design intent in [SPEC.md](SPEC.md). Update this in the same change as the code, after the verify loop (`npm test` · `npm run lint` · `npm run validate:content` · `npm run sim -- 1234 4`) passes.

**Snapshot:** 212 tests passing · content pack v2 (**0 warnings**) · typecheck clean (6 projects) · 18 campaign-stage fixtures · simulator chains TWO campaign games. Engine: the canonical base campaign plus all Pack 1-4 and Pocket 1-2 rules are executable and persisted. Web: hub → local hot-seat, LAN auth → campaigns → lobby → filtered networked game, with sealed choices represented as explicit UI decisions. The optional Do Not Open Ever packet remains disabled by design.

## Worklog (newest first)
- **2026-07-12** — Completed the **standalone PixiJS presentation-runtime migration** in the sibling `risk-legacy-table` repository. The production game route now lazy-loads one retained Pixi table scene with map-defined anchors and camera profiles, generated faction miniatures, army-stack composition, permanent marks including Alien Island, semantic event translation, skippable battle/conquest/scar/module/nuclear/victory/signing sequences, catch-up/resync behavior, reduced-motion/quality/volume/audio settings, and a synchronized keyboard-accessible DOM board. The prior React SVG piece renderer and effects layer were removed from production. Added versioned save migration, typed rules events, recording/director/interaction tests, Playwright visual baselines across seven factions, all permanent marks, three-to-five-player populations, and phone/tablet/desktop/quality/motion states, renderer diagnostics, asset/runtime budgets, and a real local-campaign route smoke test. Final verification: **239 Vitest tests across 27 files**, **10 Playwright browser tests**, a passing **30-minute active/idle memory soak** (147 heap samples, 2.6 MB drift), content/map validation, two-game simulation, lint/typecheck, asset budget report, performance benchmark, and production build all green.
- **2026-07-11** — Completed the **full campaign rules audit and sealed-content implementation**. Cross-checked both Hasbro booklets, physical-component scans from the Risk Legacy TTS workshop asset, the campaign/Mutant/Alien transcriptions, BGG FAQ, and play reports. Added deterministic stage fixtures from fresh Game 1 through post-campaign play and persistence checks through browser and PostgreSQL snapshots. Implemented the complete Pack 1 draft/Events, Pack 2 Comeback Powers, Pack 3 Homelands/Missions/Events, Pack 4 Lead Faction plus all six Private Missions, Pocket 1's complete nuclear casualty chain/Mutants/evolutions/Missile Powers/Events, and Pocket 2's collaborator/Alien Island card/Aliens/Weaknesses/Ruins/Events. Late scan-driven fixes include one Resource-card set per recruit phase, Homeland ties giving none, Lead ties giving none, the sixth Pack 4 mission, nuclear attacking and adjacent losses plus knockout discards, Ruins as rebuildable marks, conditional Die Humans destruction, and Mysterious Island's second Event. **212 tests across 20 files**; lint, content validation, simulator, and all 18 fixtures green.
- **2026-07-08** — Implemented **UI-11 — legacy-game theming pass** (capstone — closes the UI polish batch; UI-1…UI-12 all done). One CSS theming layer in `index.css` extends the board's battle-worn aesthetic across every piece of chrome without touching components: a shared `--chrome-noise` grain (code-drawn SVG turbulence, same family as the UI-9 card texture) applied to all panel surfaces (`.bg-panel`, `.bg-panel-2`, and the hub's `bg-panel/95` cards — header, rail, modals, docks, drawer, ledger), a fixed table **vignette** over the whole app (`body::after`, pointer-transparent, above the overlay layer so even modals sit "on the table"), and engraved display type (`.font-display` letterpress shadow) across headings and the phase track. Hub and game now visibly share one treatment on top of the UI-6/UI-7/UI-9/UI-10 art. **Browser check at 1280x720 (documented):** computed styles verified in a live Chromium — grain present on hub cards, game header, and rail; vignette present on hub and in-game incl. over the setup takeover; engraved type on headings (screenshot capture times out in this embedded preview, as noted under UI-4/UI-5; computed-style verification stands in). No component/test changes needed — **120 passing** unchanged; lint + validate + sim + web build green.
- **2026-07-08** — Implemented **UI-6 — game-first hub polish**. The hub now reads as the game table: the real board art renders as a dimmed full-bleed backdrop, "AN UNNAMED WORLD" sits in display type over it with the five faction emblems in a row (UI-10 components), and the two entries use the agreed player-facing names — **PLAY AT THIS TABLE** (hot-seat seats + dice seed + START GAME, copy now "pass the device", no engine-implementation blurb) and **JOIN THE WAR ROOM** (LAN connect, copy about the group's campaign — the `npm run dev:server` instruction and the `1-web-b` task reference are gone, as is the TACTICAL BOARD status card). The hub smoke test now asserts the world title, 5 emblems, both entries, and that no `1-web`/`npm run` strings render. Deferred: the connected-campaign strip (game number, signatures, opened-envelope pack icons) — the hub has no campaign data source until the LAN flow persists a last-connected campaign summary; noted for the next backlog cycle. **120 passing**; lint + validate + sim + web build green.
- **2026-07-08** — Implemented **UI-5 — responsive play layout**. A `useDesktop` breakpoint hook (min-width 1024px, with a `resize`-event fallback because some embedded webviews skip mediaquery change events) drives the game grid: **desktop** keeps board + 340px rail; **tablet/phone** go board-first — the rail vanishes and its content (inspector, sideboard mat, quick look, battle log) moves into a bottom **TABLE drawer** (max-height 70%, CLOSE tab). The phase strip re-flows below lg: HUB + acting stay on row one, the phase track wraps to its own full-width horizontally-scrollable row with tighter labels, so nothing clips at 390px. The end-of-turn draw dock wraps its card row at phone widths; the aftermath re-open button hugs the right edge without the rail offset. The jsdom shim now acts as a 1280px desktop (min-width queries ≤1280 match) so the whole existing suite exercises the desktop layout; 1 new test overrides matchMedia to phone and asserts no rail, board-first, action bar reachable, and the TABLE drawer round-trip. **Browser QA (dev server, fresh load per viewport):** 390x844 → drawer layout, `scrollWidth == innerWidth` (no horizontal overflow) across hub/takeover/board/drawer; 768x1024 → same; 1280x720 → rail layout, no TABLE button, no overflow. (Live viewport *resizes* don't re-dispatch events in the embedded preview — an emulation artifact; fresh loads at each size verify the real behavior. Screenshots time out in this environment; DOM/computed metrics documented instead.) **120 passing**; lint + validate + sim + web build green.
- **2026-07-08** — Implemented **UI-4 — board legibility pass** (generated board stays active; manifest-driven renderer + click behavior unchanged). Generator (`generate-board.ts`, board regenerated): territory labels scaled ×1.16 (all 42 now ≥6.1 viewBox units — asserted in the board contract test), route lines thickened 1.35→1.85 at full opacity with larger nodes. Overlays (`Board.tsx`): troop counters scaled ×1.15 with the count font 9.5→10.5, HQ emblem shields enlarged (r 9.6→10.6), city markers bigger/higher-contrast, owner tint stroke 2.2→2.8. Highlight states (`index.css`) are now distinct in **color + width + dash pattern** with per-state glow: selected = solid amber 3.2 w/ glow, attack = red long-dash 2.8, move = green dot 2.6, start = amber short-dash 2.4. **Browser QA at 1280x720** (dev server, real Chromium): troop counts render ≈12.7px and labels ≈8.7px on screen (readable without zoom); all four highlight styles verified computed-distinct on a live path (initial reads were confounded by the throttled preview renderer freezing the 120ms stroke transition — verified with transitions disabled); a full setup was driven through the takeover in-browser, incidentally confirming the UI-3 illegal-start absorption end-to-end. Also committed a `.claude/launch.json` dev-server config. **119 passing**; lint + validate + sim + web build green.
- **2026-07-08** — Implemented **UI-3 — territory inspector and move preview**. New `Inspector.tsx` tops the rail whenever a territory is clicked (new `ui.inspected`, set on every board click, including view-only): territory + continent names, owner with emblem, troops, HQ (with red-star note), city (type/name/population), scars, fortification durability. A **NEXT CLICK** block previews the current phase's action for the acting player: setup start legality (with the specific reason when illegal — claimed/city-marked/scar-marked/adjacent-HQ), join re-entry, recruit placement count, attack-source selection with a per-neighbor target list (attack with defender count, expand with troop count, "locked this turn (Defensive Stand)", "needs 2+ troops"), declaration/expansion previews when inspecting a neighbor of the selected source, and maneuver connectivity via `maneuverNetwork` ("unreachable — maneuvers travel only through territory you control"). **Setup behavior change:** clicking an illegal start no longer dispatches (the engine rejection toast) — the click is absorbed, an error points at the territory panel, and the inspector explains why. 2 new tests (attack-source verdict list incl. defender counts, then the commit opening the combat overlay; illegal setup click absorbed with reason + legal click still placing) → **119 passing**; lint + validate + sim + web build green.
- **2026-07-08** — Implemented **UI-12 — victory, signing & reward flow presentation** (the first web reward UI — the engine flow existed since 10a with no surface). New `VictoryFlow.tsx` mounts at `game_over`: (1) **victory beat** — full-screen takeover with the winner's lg emblem + win reason; (2) **signing moment** — the winner's name rendered in a handwriting face (`.signature-script`) with the signature count (engine already auto-signed; presentation only); (3) **reward sequence** — a `RewardModal` sticker-sheet walking the engine's claim order (winner sheet of 5 / held-on sheet of 2 + PASS) with the whose-decision chip, remaining inventory per choice, ineligible/depleted choices dimmed with the reason, and the winner's PASS gated by a client-side mirror of `anyWinnerRewardAvailable`; (4) **target selection drops to the board** — new `ui.rewardTarget` mode glows eligible territories, cancel-scar/fortify dispatch on click, continent rewards map the clicked territory to its continent (+1/−1 toggle for bonus marks honoring the campaign-wide once-each rule), and naming rewards collect the name in a board banner before CONFIRM; the card upgrade picks from the claimant's eligible territory cards rendered as UI-9 components; (5) **aftermath** — per-faction result rows with emblems and a world-change recap derived from post-win events, preceded by a code-drawn **envelope tear-open reveal** whenever `ModuleRevealed` fired ("DO NOT OPEN…YET" → TEAR OPEN → module names). **Scar play** landed on the same layer: held scars render as full-art `ScarCard`s in the hand strip; clicking arms `ui.scarTarget` (stable-boundary gated), the board glows unscarred territories, and the play lands as the UI-10 circular chip. Fixture: `driveToVictory(seed)` bot-drives a real game to `game_over` with rewards open. 3 new tests (full beat→signing→claim-order→board-named-continent→aftermath ritual; synthetic module unlock envelope; scar card→chip flow) → **117 passing**; lint + validate + sim + web build green.
- **2026-07-08** — Implemented **UI-2 — action-first side panel** (rescoped: bottom = you, right = the table). New `ActionBar.tsx` renders the non-blocking phase controls in the UI-9 bottom strip beside the hand/HUD: recruit (compact breakdown, to-place count, place-per-click stepper, TRADE using strip-selected cards, TO ATTACK), join-the-war hint, expand/attack (expand-count stepper, END ATTACKS), maneuver (move-count stepper, END MANEUVER); it renders only for the acting player. The rail (`SidePanel.tsx`) is now purely ambient — RESULT (game over) → sideboard mat → Quick Look roster — with zero dispatch surface. `Ledger.tsx` became a collapsible **BATTLE LOG tab** at the rail's foot (collapsed by default, event count in the header) and got the perf fix: renders only the latest 50 events with a lazy "— earlier N events —" expansion instead of the full log per action (the quadratic long-game render noted 2026-07-06). 2 new GameScreen tests (recruit controls in the action bar + rail asserted control-free, driving recruit→attack through the real engine; log tab collapsed/windowed/expanded) → **114 passing**; lint + validate + sim + web build green.
- **2026-07-07** — Implemented **UI-10 — faction visual identity**. New `factionAssets.ts` registers the committed emblem PNGs (`assets/factions/*.png`), scar art (`assets/scars/*.png`), per-faction blurbs (UI copy), and per-faction troop-marker silhouettes; new `FactionEmblem.tsx` renders emblems as framed tiles with a faction-color border (dark art backgrounds), circular-cropped at xs/sm sizes, with a styled monogram fallback for module factions without art. Surfaces: the UI-8 setup takeover's faction buttons are now full **faction cards** (emblem, name, blurb, both readable powers; the power step shows the lg emblem), the rail Quick Look roster and hand-strip HUD use xs circular emblems, and the combat overlay header shows attacker/defender sm emblems beside the faction-colored panels. Board (beyond color): **HQ markers are circular emblem shields** (clip-pathed PNG in a faction-color ring), **troop markers use distinct per-faction silhouettes** (square/hex/diamond/circle/shield), and **scars render as circular art chips** (danger-ringed clip-pathed scar PNG, replacing the red `!` rect). 4 new tests (distinct art ×5 + fallback, setup faction cards, roster+combat emblems, HQ shields + ≥3 distinct troop shapes) → **112 passing**; lint + validate + sim + web build green.
- **2026-07-07** — Implemented **UI-9 — rendered resource cards + card flows**. New `apps/web/src/game/cards/`: `ResourceCard.tsx` — physical-styled card faces (yellow name banner, textured gray art panel with the board's own territory silhouette from new `@risk/map` exports `territoryPath(id)`/`continentColors` — cards and board share one geometry+palette source; yellow lower panel with a 3×2 grid of six coin slots, filled = current resources incl. `cardModifications` upgrades via new `cardResources(gs, id)`; small card id in the corner), coin cards as one big coin face, and a black star-logo back for decks/hidden hands; upgraded coins render identically to base coins (physical-sticker rule, asserted in test). Textures are code-drawn SVG noise behind the single `texture.ts` seam (`CARD_TEXTURE_URL`) so a scanned image can drop in later. `SideboardMat.tsx` renders the rail sideboard as the rulebook-style mat: DRAW stack · face-up COIN pile · sealed MISSION/EVENT outlines · DISCARD top card, numbered slots 1→4 as cards, red-star pool chip beside. New `HandStrip.tsx`: always-visible strip along the bottom of the board with YOUR hand as cards + personal HUD (red stars, missiles, scar count, recruit to-place); cards become selectable during the Red Star buy and recruit trade-in (rail/dock hold the action buttons); the strip only ever renders its own player's faces. The end-of-turn draw dock now shows the four slots as cards with TAKE under mandatory matches and disables TAKE COIN while a match exists; `BottomDock` moved into the board column's flow above the strip so hand selection stays reachable. Shared engine-driven test fixtures extracted to `test-fixtures.ts` (incl. `conquerOutpost`). **Found/fixed environment gap:** the worktree had no workspace links, so `@risk/*` resolved to the main checkout's packages — `npm install` in the worktree now links its own packages. 5 new tests (card faces/pips/upgrade/back, full draw flow, filtered hidden-hand guarantee) → **108 passing**; lint + validate + sim + web build green.
- **2026-07-07** — Implemented **UI-8 — modal/overlay layer + combat modal**, with **UI-1 — human-readable setup labels** folded in. New shared overlay layer (`apps/web/src/game/overlays.tsx`: full-screen takeover, centered overlay, bottom dock, whose-decision chip, shared Btn/Num) mounted from `GameScreen`. **Setup** now runs through a full-screen faction/power takeover (`SetupTakeover.tsx`): faction cards show readable power names from the pack (UI-1 — no more `fortified_hq` strings), picking a first-play faction opens a power step with name + rules text, and the takeover hands off to the board with an HQ-placement hint banner (CHANGE reopens it); stale picks reset when control passes. **Combat** gets the large centered overlay (`CombatOverlay.tsx`, rail `CombatTray` retired): attacker/defender panels in faction colors, big dice with a CSS tumble (`prefers-reduced-motion` respected) showing natural→final missile states, dice-count choices, the missile window as an explicit bordered interrupt, move-in slider, per-roll BATTLE LOG for sieges (scoped by the battle's `AttackDeclared` seq), ATTACK AGAIN re-arm + WITHDRAW after a non-conquering roll, a per-player "auto-defend with max dice" toggle (off by default, auto-dispatches the defender dice choice), and explanatory badges for Bunker/Ammo/Fortification/Fortified HQ/Intimidation/Defensive Stand. **Start-of-turn** (Red Star buy) and the **end-of-turn mandatory-match draw** moved to a bottom-anchored dock (`TurnDecisionDock.tsx`); the coin draw is disabled while a matching face-up card exists. New `labels.ts` (UI-1) supplies territory/continent/power/scar/card labels pack- and manifest-driven; SidePanel + Ledger now use them. 3 new combat/dock tests + the setup test rewritten against the takeover (readable-label assertions) → **103 passing**; lint + validate + sim + web build green. Deferred: reward/scar-play surfaces on the overlay layer (UI-12), moving the remaining rail phase controls to the bottom action bar (UI-2).
- **2026-07-07** — Implemented **UI-7 — real board art integration** with a rebuilt SVG board at `packages/map/assets/board.svg`: the supplied Risk board SVG was normalized into canonical territory paths, then restyled with Risk Legacy-inspired continent colors, thick white country borders, route nodes/lines, fitted territory labels, top slots, a left signature rail, and six continent bonus callouts with leader lines. Removed the small white scar/sticker placeholder marks from the source-art treatment. The board keeps the required 42 `path.territory-border` ids on the stable `risk-board-modern` viewBox. `Board.tsx` renders that SVG as the art layer, binds click/highlight/owner state onto the raw paths, and keeps React-owned troop/HQ/city/scar/fortification overlays aligned from the extracted territory anchors; the board and Pixi effects now share one aspect-ratio frame so canvas effects do not drift at tablet widths. Added Vite raw-SVG typing and component tests for the imported board contract plus a real setup click through an SVG territory path. Browser QA: desktop board/overlays verified at 1280x720; tablet viewport 768x1024 verified after pinning the SVG/overlay to `xMidYMin`. **100 tests passing**; lint + validate + sim + web build green.
- **2026-07-06** — Implemented **13 — polish / PixiJS effects**. `EffectsLayer` swapped from raw canvas 2D to **PixiJS v8** at the existing stable seam (identical props; zero game-component changes): attack beam on `DiceRolled`, conquest pulse on `TerritoryConquered`, animated via `app.ticker` with `Graphics`, `resizeTo` the host div, `prefers-reduced-motion` respected. Init is async and failure-tolerant — with no WebGL/WebGPU (jsdom tests, old machines) the layer degrades to an empty overlay, so effects never gate play; the test shim now returns `null` for non-2D contexts so Pixi fails init cleanly. Pixi's renderer chunks code-split in the build (main bundle 172 KB gz). **92 tests passing**; full verify loop + web build green. **This closes the original backlog** — remaining future work (module mechanics from host-entered text, real board art, networked-play niceties) is queued in BACKLOG's "Ready right now" section.
- **2026-07-06** — Implemented **12 — import wizard** (`content_required` card text). Engine: new pure `supplyModuleContent(campaign, moduleId, item, content)` in `campaign.ts` — stores the host's text in new `CampaignState.hostContent` (keyed `${moduleId}.${item}`, SPEC §10 updated), clears the item from `contentRequired` (module entry drops when empty), rejects unknown module/item; supplying Pack 1's `draft` lifts the `createGame` advanced-draft gate while other pending items don't block. Server: `GET /api/campaigns/:id/state` (members; legacy summary incl. `contentRequired`) and host-only `POST /api/campaigns/:id/content` — applies the supply, persists `campaigns.state`, appends an immutable `content_overrides` row, audits `ModuleContentSupplied`. Web: the lobby shows a **SEALED CONTENT REQUIRED** wizard to the host whenever the campaign has paused unlocks — one textarea per `moduleId · item` with per-item SUPPLY; the panel clears as the server confirms. 1 engine + 1 pg-mem + 1 component test → **92 passing**; full verify loop + web build green. Note: the wizard stores and unblocks; the *mechanics* consuming the entered text (advanced-draft turns, comeback-power effects, missions) are future work that starts when the group actually unlocks those modules with real card text.
- **2026-07-06** — Implemented **1-web-b — networked game screen**. The game UI was extracted from `SandboxGame` into a shared `GameScreen` (phase strip, board, side panel, ledger, all interaction) — the hot-seat wrapper keeps the local engine, and new `NetworkedGame` drives the same screen from per-viewer filtered `game:state` payloads, sending `game:action` with server `RuleViolation` acks surfaced as the error toast; a `viewer` prop makes controls live only when `waitingOn` is you (spectators are view-only). **`filterState` moved from the server into `@risk/rules` (`filterStateFor`, typed `FilteredGameState`)** so client tests exercise the exact server policy; SidePanel now prefers filtered count fields (`handCount`, deck/coin counts). `LanApp` mounts `NetworkedGame` when `game:create`/`game:created` yields a session. Reachability test (`NetworkedGame.test.tsx`): an authoritative in-test session (real engine + real filter) with three seats — two mounted clients + one headless — plays a **full game to `game_over` + committed rewards** through the socket protocol: the first pick is click-driven through the real UI (faction → power → board territory), acting for another player is rejected by the server guard, every broadcast payload across the whole game is leak-checked (0 hidden-hand/scar violations, with hands verifiably non-empty), and fresh clients re-join to render the same winner. UIs unmount during the long drive because the Ledger renders the full event log per action (quadratic in test time); noted as a future UI perf item. **89 tests passing**; lint + validate + sim + web build green. Deferred: reconnection UX, optimistic updates, true socket/Postgres e2e (manual on the LAN — the in-test session covers engine+filter+protocol semantics).
- **2026-07-06** — Implemented **1-web-a — auth + campaign/lobby UI**. New `apps/web/src/net/`: `api.ts` (thin REST client; token in React state only — no browser storage) and `LanApp.tsx` (three-stage flow: sign-in/register with configurable server URL → campaign list/create/join-by-invite with host invite codes → Socket.IO lobby with live presence via `lobby:state`, ready toggle, and host-only `game:create` launch; `game:created` broadcast shows the session banner — the synced game screen itself is 1-web-b). Hub gained a CONNECT button; `App.tsx` now routes hub / local sandbox / LAN views. **Server fix:** the CORS middleware never answered `OPTIONS` preflights (and omitted `Allow-Methods`), so every browser JSON/Authorization request would have 404'd — preflights now return 204. 3 component tests (`LanApp.test.tsx`, mocked fetch + Socket.IO): register→campaign list with invite code, server login error surfaced, lobby open→ready toggle round-trip→host launch reaching `game:create` with the ack session id → **88 passing**; full verify loop + web build green. Deferred: reconnection/edge UX; spectator flows.
- **2026-07-06** — Implemented **11 — unlock module engine** (test-first; SPEC §9). New reveal machinery in the engine: `GameState.unlockedModules` (seeded from campaign) + `pendingUnlocks`; `queueUnlock` emits `ModuleTriggered` and reveals per timing — **mid-game immediately, end-game after the last reward resolves** (also flushed post-Game-15 when no reward flow opens); multiple pending unlocks process in the canonical manifest order (Pack 1→2→3→4→Pocket 1→2); `ModuleRevealed` + `ModuleContentRequired` (host-entered card text pause, consumed by task 12) are emitted per module. Triggers wired: **Pack 2** on elimination — mid-game on a failed Join the War, end-game when a knocked-out faction is classified `eliminated` at victory (both SPEC branches); **Pack 1** when the 9th (last) Minor City is founded (`inventories.minorCities` hits 0), end-game reveal. Permanent effects: **Mercenary flipped to `confirmed`** (SPEC §5) and wired (`add_troops` at controller's end of turn, `ScarReinforcement`); module scars (Biohazard ×3 / Mercenary ×3, via new pack `scarSource` fields) join the campaign scar inventory when their module folds, join the setup deal pool, and become playable via `scar.play` (sealed-module scars rejected); **Pack 1's advanced-draft gate**: `createGame` refuses to start a campaign game while `pack_1` draft values are host-missing (D5: the draft replaces base roll setup). `CampaignState.contentRequired` added (SPEC §10 updated) and folded from revealed modules. Content pack now validates with **0 warnings**. Reachability: seed 1234/4p — game 1's elimination reveals Pack 2 after rewards; game 2 seeds with 3 Mercenary instances in the deal pool and converges. 8 new tests (`unlocks.test.ts`) → **85 passing**; lint + validate + sim green. Deferred: Pack 3/4/Pocket 1/2 triggers (hooks exist; their content is host-entered or depends on module state like the World Capital), comeback-power selection flow (content_required, task 12).
- **2026-07-06** — Implemented **9 — faction power handlers** (test-first, all ten SPEC §6 powers). Pack: the ten placeholder power ids replaced with the official ids (`fortified_hq`/`defensive_stand`, `lower_die_intimidation`/`total_conquest`, `round_up_recruiting`/`expansionist_supply`, `hq_reinforcement`/`territory_card_reinforcement`, `early_maneuver`/`unconnected_maneuver`), all `confirmed` (content warnings 11→1); powers schema is now passthrough so per-power effect data (e.g. `expandThreshold: 4`) lives in the pack. Selection: powers attach to the **faction** — `setup.choose` takes `powerId` (required on a faction's first play, `FactionPowerChosen`; campaign-stored choices are permanent and re-picks are rejected); choices fold into `CampaignState.factionPowerChoices` (10b). New per-turn state (`blockedAttackTargets`, `intimidation`, `expandedThisTurn`) resets in `enterStartTurn`. Handlers: fortified_hq (+1 each defense die on DM's own-HQ territory, no durability, no stacking with the Fortification mark), defensive_stand (natural [6,6] defense locks the territory from new declarations for the turn), lower_die_intimidation (−1 lower defense die at Enclave's first target until it attacks elsewhere; missile dice untouched; clamp 1..6), total_conquest (natural three-of-a-kind + ≥1 final kill wipes all defenders, conquest proceeds normally), round_up_recruiting (ceil on the recruit division), expansionist_supply (4+ unoccupied expansions earn the end-of-turn draw without a conquest), hq_reinforcement (+1 troop per controlled HQ territory at turn start), territory_card_reinforcement (optional `khanReinforce` flag on `end.draw`), early_maneuver (the one maneuver at any stable own-turn point, combat closed), unconnected_maneuver (connectivity waived). Every application emits `FactionPowerApplied`. **Rules correction:** recruit is now `max(floor((territories + population) / 3), 3)` — population counts INSIDE the division (rulebook; implied by SPEC §6 round_up wording; new pack constant `recruitCountsPopulationInDivision`; previously population was added at face value). SPEC §4 table now states the formula. Web sandbox: setup gained a starting-power picker (first play) / permanent-power label (later games); recruit breakdown display updated. Simulator: picks each faction's first power and reports `powers applied` per game (seed 1234/4p: fortified_hq + intimidation fire in game 1; khan wins game 2 with hq_reinforcement×4) — reachability for combat + recruitment powers; trajectory changed as expected (powers shift balance; both games converge). 16 new engine tests → **77 passing**; lint + validate + sim green. Deferred: sandbox HUD affordance for early/unconnected maneuver (engine supports both; UI still drives only the maneuver-phase connected flow).
- **2026-07-06** — Implemented **TEST-web — minimal web test harness**. Vitest + React Testing Library + jsdom wired into the root `vitest.config.ts` (web tests included via `apps/web/src/**/*.test.{ts,tsx}`, JSX automatic runtime enabled; the test file declares `@vitest-environment jsdom`). `apps/web/src/test-shims.ts` fills jsdom gaps the app touches (`matchMedia`, canvas 2D context, `requestAnimationFrame`, `Element.scrollTo`) so components mount unchanged. 3 smoke tests in `App.test.tsx`: hub renders; START GAME mounts the sandbox against the real engine (board contract asserted: `#risk-board-modern`, 42 `path.territory-border`, exact viewBox) with setup routed to the first chooser; ← HUB exits back. **61 tests passing**; full verify loop green. Web tasks (`1-web-a/b`, `13`) now have machine-verifiable acceptance.
- **2026-07-06** — Fixed **BUG-1**: `game:create` now requires **3+ ready players** (was 2+), matching the engine's `createGame` minimum — a 2-player lobby previously created a session that crashed on replay. One-line guard in `apps/server/src/main.ts`; no socket-level test harness exists yet (see TEST-web), so verified by the type-checked guard + full verify loop. Test count unchanged (58).
- **2026-07-06** — Implemented **10b — cross-game campaign persistence** (test-first). New `packages/rules/src/campaign.ts`: canonical `CampaignState` (SPEC §10, with `continentNames` extended to carry `namedBy` so the §7 personal +1 survives across games — SPEC updated), `initialCampaign(worldName)` (inventories from pack data incl. per-scar instance counts), and pure `applyGameToCampaign(campaign, finished)` (folds board scars/cities/fortifications, continent names/marks, card upgrades + destroyed cards, signatures, reward inventories, per-faction result history incl. `unused`, `foundedMinorCities`; scar instances are consumed by `ScarPlayed` events so unplayed dealt scars return). `createGame` now takes `campaign?`: seeds board legacy before placement (`CampaignLegacyApplied`), overlays card upgrades, keeps destroyed cards out of the deck, carries reward inventories, deals starter scars from the campaign's remaining instances, and applies **signature-driven setup** (0 signatures → 1 token/0 missiles; ≥1 → 0 tokens/missiles = count). New `GameState.gameNumber`; rewards (incl. signing) stop after Game 15 (`starterRewardsLastGame` pack constant, `EndGameRewardsSkipped`). `isLegalStart` now treats **scars as marks** (own founded Major City stays legal even if scarred) — latent-rule fix that previously allowed starts/rejoins on scarred territories. Server: migration `002_campaign_state.sql` (`campaigns.state`, `game_sessions.campaign_state`); `game:create` snapshots the campaign onto the session row (replay stays deterministic), `loadSession` seeds from it, and when the last reward resolves the game **folds into `campaigns.state`** (`CampaignStateFolded` audit, export now includes the folded campaign). Also fixed the completion block to fire once at the winning action instead of re-running on every post-win `reward.choose`. Simulator now chains a second game seeded from the folded campaign (game 1 winner starts game 2 with a missile, cities carry; both games converge; nonzero exit if game 2 stalls). 7 engine tests (`campaign.test.ts`) + 1 pg-mem server test → **58 passing**; lint + validate + sim green. Deferred: web UI for campaign continuity display; unlock flags carry but nothing reads them until task 11.
- **2026-07-06** — Implemented **10a — end-game reward placement / signatures** (test-first). Victory now auto-signs the board for the winner (new `GameState.signatures`, `BoardSigned` event) and opens a post-win reward flow inside the locked `game_over` phase (`rewards: { order, nextIdx, committed }` — winner first, then held-on non-winners clockwise from the winner; eliminated/unused factions get nothing). New `reward.choose` action implements all seven SPEC §7 rewards: `name_continent` (once per continent; the namer's personal +1 wired into `recruitBreakdown.namedBonus`), `found_major_city` (any city-less territory, even enemy-held), `cancel_scar`, `change_continent_bonus` (one +1 and one −1 campaign-wide, each continent at most once; global at recruitment via `globalModifier`), `fortify_city` (sets/replaces a city fortification at 10 durability), `found_minor_city` (territory the claimant controls at game end), `upgrade_territory_card` (+1 resource, max 6, coins never; recorded in new `GameState.cardModifications`, which `recruit.trade` now honors), plus `pass` (held-on freely; the winner only when no winner reward is resolvable). Finite inventories (4 cancel / 5 fortify / 5 major / 9 minor) live in pack data as new confirmed rule constants (`rewardInventories`, `namedContinentBonus`, `cardUpgradeMaxResources`). `EndGameRewardsCommitted` fires when the last claimant resolves (unlock-engine hook point for task 11); `waitingOn` routes to the current reward claimant. The simulator drives the whole reward flow through the real action API after the win (reachability: seed 1234/4p — khan wins, signs, names a continent; 3 minor cities founded; pre-win trajectory unchanged at 202 steps since rewards consume no seeded RNG). 10 new engine tests → **50 passing**; lint + validate + sim green. Deferred: sandbox/web UI for reward selection (server `filterState` already passes the public reward state through unfiltered — it's all public); carrying the recorded legacy state into the next game is 10b.
- **2026-06-29** — Implemented **`city` — city model & mechanics** (test-first). `TerritoryState.city` is now the full SPEC §7 model `{ type: "minor"|"major"|"world_capital"; population; name?; foundedByPlayerId? }` (was `{ name?; population }`). Added `cityPopulation(type)` content accessor reading `cityPopulations` from pack data. `isLegalStart` now keys the Major-City allowance on `city.type === "major"` + `foundedByPlayerId === placingPlayerId` (threaded a new `placingPlayerId` param through `setup.choose`, the `start.done` rejoin check, and `join.enter`) instead of the old `(t as any).foundedBy === placingFaction` / `population === 2` hack. **Fixed a latent bug:** `unoccupiedUnmarked` now treats *any* city as a mark, so a non-founder can no longer start on another player's empty Major City (previously the city was invisible to that check). Population recruitment + expand resistance already read `city.population`, now exercised by tests. 3 new engine tests (population recruit, founder-only Major-City legal start + minor-never, minor/major expand resistance via the real `attack.expand` path) → **40 passing**; lint + validate + sim green. Founding action itself remains task 10a. Note: the two scar-suite city literals were updated to the new model.
- **2026-06-13** — Resolved decisions **D1–D5** (group-supplied) and recorded them in SPEC §5 (scars), §6 (faction powers), §7 (cities/rewards/signatures/carry-over), §9 (unlock modules), §10 (CampaignState). Reclassifications captured: Fortification is a winner reward mark (not a scar); Biohazard→Pack 1, Mercenary→Pack 2; starter playable scars = Bunker + Ammo only; starting Red Stars are signature-driven; pack faction-power ids are placeholders to be replaced in task 9. Flipped all D-blocked tasks to unblocked in BACKLOG and re-decomposed them.
- **2026-06-13** — Implemented **8b — scar play action** (test-first). Pack: scar data reclassified with `source`/`starterPlayable`/`instances` (Bunker ×3 + Ammo ×3 starter inventory; biohazard/mercenary/fortification not starter-dealt). Engine: `createGame` deals one hidden instance per player from the starter pool when enough remain (new `player.scarHand`, identity hidden, `scarCardCount` public); new `scar.play` action playable by the **holder on any player's turn** at a stable boundary (rejected during a post-roll missile window or conquest move-in), targeting any unscarred territory, one scar per territory, emitting `ScarPlayed`; defense dice now **clamped 1..6** after scar modifiers (D1). Server `filterState` hides held-scar identities (count only). 7 new engine tests incl. a reachability case (Bunker placed via the real action then fires in a later defense roll) → **37 passing**; lint + validate green. **8a scar effects are now reachable in real play.** Note: the sim trajectory for seed 1234/4p changed (was 70 steps/imperial; now 202 steps/khan) because the new scar deal draws a different amount of seeded RNG at setup — still deterministic (identical across runs) and converges to a 4-star winner.
- **2026-06-13** — Restructured the docs for an agentic loop. Split the conflated "spec/plan/tracker" into three artifacts: **SPEC.md** (stable design intent, status-free), **BACKLOG.md** (new — the single machine source of truth: dependency-aware tasks with stable ids, explicit `Blocked by`, and acceptance criteria that require *reachability* in real play, not just unit tests), and this file (worklog only). Moved live status into BACKLOG (removed the slice table here); the "Scar play rules" spec block now lives in SPEC §5. Pointed CLAUDE.md / README.md / PLAN-r2.md at BACKLOG instead of restating status, killing the four-place duplication. Captured a decision queue (D1–D5) to resolve one at a time. Also logged two findings from the code audit as backlog items: `BUG-1` (server allows <3 players, which the engine rejects → crash on replay) and the fact that **8a scar effects are currently unreachable** in real play until the scar play action (8b) exists. No engine code changed.
- **2026-06-12** — Recorded scar play/placement rules supplied by the human (now folded into SPEC §5): valid targets (territory/faction/city/HQ/player), the 6-step play sequence, persistence across games, and the eight `ScarTiming` windows. Resolved the long-standing placement-rules decision; the remaining gap is the per-card window/target assignment (BACKLOG D1).
- **2026-06-12** — Slice 8 effects done: wired the four confirmed scar effects into the engine. Dice modifiers (Bunker +1 highest def die, Ammo Shortage −1 highest, Fortification +1 each) applied to the defender's natural dice in `finalDice`/`resolveCombat`, reading amounts from pack data; missile dice stay unmodifiable (a scar never alters a missile-set 6; a "highest die" bonus targeting a missile die is lost, not shifted). Fortification durability: −1 box per roll made with exactly 3 attacker dice (`FortificationDurabilityMarked`), field cleared + `FortificationExpired` at 0. Biohazard: `ScarAttrition` (−1 troop) on both `end.turn` and `end.draw` advancement for territories the active player controls; territory vacated (controller cleared) if the last troop is lost. 8 new engine tests → **30 passing**; full verification loop green (sim seed 1234/4p unchanged: 70 steps, imperial_balkania wins). Deferred: scar PLAY action.
- **2026-06-12** — Added agent handoff: `CLAUDE.md` (verification loop, conventions, guardrails, slice-selection + tracking procedure) and this tracker. Renamed repo folder to `risk-legacy-web`.
- **2026-06-12** — Rules v2: corrected against rulebook. Flat 8 starting troops; 3–5 players (2p rejected); Join-the-War = half (4), no HQ; initial HQ not adjacent to another HQ; fortified expand +2; scar effects specified in data (Bunker/Ammo/Biohazard/Fortification durability 10); 7-module sealed manifest seeded. Simulator bot reworked to seek enemy HQs + expand → converges. 22 tests passing.
- **2026-06-11** — Initial build: monorepo scaffold, map manifest + placeholder board, content pack + zod validation, deterministic event-sourced rules engine (setup→victory) with tests, lean server (auth/campaigns/lobbies/ledger/filtering), Vite/React hot-seat sandbox, simulator harness. 21 tests passing.
